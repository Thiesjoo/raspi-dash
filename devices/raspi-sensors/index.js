const internal = require("./internal")


async function init() {
    //Create a few sensors:
    // - Cpu tmep
        // - Temp sensor
    // - (Movement, keypad, light, humidity)


    
}

async function loop() {
    let cpu = await internal.getCPUTemp()


}